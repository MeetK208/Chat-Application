<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Sidebar 04</title>

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Poppins:300,400,500,600,700,800,900" rel="stylesheet">
    <!-- Stylesheets -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
      /* General Styling */
      body {
        font-family: 'Poppins', sans-serif;
        background-color: #f8f9fa;
        margin: 0;
        padding: 0;
      }

      .wrapper {
        display: flex;
        min-height: 100vh;
      }

      #sidebar {
        background-color: #343a40;
        color: #fff;
        width: 250px;
        height: 100vh;
        position: fixed;
        top: 0;
        left: 0;
        overflow-y: auto;
        transition: all 0.3s ease;
      }

      #sidebar.active {
        margin-left: -250px;
      }

      .custom-menu {
        padding: 10px;
        background: #343a40;
        text-align: right;
      }

      .custom-menu .btn {
        color: #fff;
        border: none;
        background: transparent;
        font-size: 20px;
      }

      .logo {
        font-size: 24px;
        font-weight: 600;
        color: #fff;
        text-decoration: none;
      }

      .components {
        padding: 20px;
        list-style: none;
        margin: 0;
      }

      .components li {
        margin-bottom: 15px;
      }

      .components li a {
        color: #fff;
        text-decoration: none;
        display: flex;
        align-items: center;
      }

      .components li a:hover {
        color: #adb5bd;
      }

      .components .active a {
        color: #007bff;
      }

      .components .fa {
        margin-right: 10px;
      }

      #content {
        margin-left: 250px;
        padding: 20px;
        width: calc(100% - 250px);
        transition: all 0.3s ease;
      }

      #content.p-md-5 {
        padding: 30px;
      }

      #content.pt-5 {
        padding-top: 40px;
      }
    </style>
</head>
<body>

<div class="wrapper d-flex align-items-stretch">
    <nav id="sidebar">
        <div class="custom-menu">
            <button type="button" id="sidebarCollapse" class="btn">
                <i class="fa fa-bars"></i>
                <span class="sr-only">Toggle Sidebar</span>
            </button>
        </div>
        <h1><a href="<%= homeRoute %>" class="logo">Chat Application</a></h1>
        <ul class="list-unstyled components mb-5">
            <li class="active">
                <a href="<%= homeRoute %>"><span class="fa fa-home mr-3"></span> Dashboard</a>
            </li>
            <li>
                <a href="<%= logoutRoute %>"><span class="fa fa-sign-out mr-3"></span> Logout</a>
            </li>
        </ul>
    </nav>

    <!-- Page Content -->
    <div id="content" class="p-4 p-md-5 pt-5">
        <!-- Your content here -->
